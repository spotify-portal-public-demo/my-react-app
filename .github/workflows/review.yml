name: Review build

on:
  pull_request:
    branches: ["main"]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install
      - name: Build Next.js app
        run: npm run build
